/**
 * Application Constants
 * Centralized configuration values used across the application
 */

// Default profile picture path for users without a custom avatar
export const DEFAULT_AVATAR_PATH = '/uploads/default/default-avatar.png';

// Reel duration limits (in seconds)
export const REEL_DURATION_VERIFIED = 60;
export const REEL_DURATION_NORMAL = 40;
